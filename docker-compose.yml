version: '3.8'

services:
  bot:
    build: .
    container_name: python_telegram_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DB_PATH=/app/bot.db
    volumes:
      - ./bot.db:/app/bot.db
